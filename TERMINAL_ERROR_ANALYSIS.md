# 터미널 오류 분석 및 해결

## 🔍 발견된 문제

### 오류 1: new.h 파일을 찾을 수 없음

**오류 메시지:**
```
fatal error C1083: 포함 파일을 열 수 없습니다. 'new.h': No such file or directory
파일: afx.h(62,11)
```

**영향을 받는 파일:**
- 모든 .cpp 파일 (StdAfx.cpp, ETrade_Order_Sample.cpp, etc.)

**원인:**
- Visual Studio 2022의 MFC (`afx.h`)가 `new.h`를 포함하려고 시도
- VS 2022에서는 `new.h` 파일이 없고 `<new>` 헤더만 존재
- `afx.h` 내부: `#ifndef _INC_NEW` 체크 후 `#include <new.h>` 시도

---

## ✅ 해결 방법

### 적용된 수정

**StdAfx.h 파일 수정:**

```cpp
#define VC_EXTRALEAN

// Visual Studio 2022 호환성을 위한 헤더
#define _INC_NEW            // new.h 포함 방지
#include <new>              // 표준 C++ new 헤더
#include <afxwin.h>         // MFC (이제 new.h를 찾지 않음)
```

**변경 이유:**
- `afx.h`가 `#ifndef _INC_NEW` 조건을 체크
- `_INC_NEW`가 정의되어 있으면 `new.h` 포함 시도 안 함
- VS 2022에서는 `<new>` 헤더만 사용

### 추가 조치

1. **PCH 파일 삭제 완료**
   - `.pch` 파일 삭제
   - Precompiled Header 재생성 필요

2. **빌드 캐시 정리**
   - 중간 파일 삭제 권장

---

## 🚀 다음 단계

### Visual Studio에서:

1. **빌드 > 솔루션 정리** (Build > Clean Solution)
2. **빌드 > 솔루션 다시 빌드** (Ctrl+Alt+F7)

또는:

1. **프로젝트 우클릭 > 정리**
2. **프로젝트 우클릭 > 다시 빌드**

### 명령줄에서:

```powershell
# Developer Command Prompt에서
msbuild MMMaster.sln /t:Clean
msbuild MMMaster.sln /t:Rebuild /p:Configuration=Release /p:Platform=Win32
```

---

## 📋 확인 사항

빌드 전 확인:

- [x] `StdAfx.h`에 `#define _INC_NEW` 추가됨
- [x] `StdAfx.h`에 `#include <new>` 추가됨
- [x] PCH 파일 삭제됨
- [ ] 프로젝트 속성에서 MFC 설정 확인
- [ ] 빌드 정리 완료

---

## 🔧 추가 문제 해결

### 여전히 오류가 발생하면:

#### 1. 프로젝트 속성 확인

**Visual Studio에서:**
1. 프로젝트 우클릭 > **속성**
2. **구성 속성 > 일반**
   - **MFC 사용**: "동적 라이브러리에서 MFC 사용" 확인
   - **플랫폼 도구 집합**: "v143" 확인

#### 2. 수동으로 PCH 재생성

**Visual Studio에서:**
1. `StdAfx.cpp` 선택
2. **속성 > C/C++ > 미리 컴파일된 헤더**
   - **미리 컴파일된 헤더**: "생성(/Yc)" 확인
   - **미리 컴파일된 헤더 파일**: "StdAfx.h" 확인

#### 3. IntelliSense 오류 무시

일부 경우 IntelliSense만 오류를 표시하고 실제 빌드는 성공할 수 있습니다.
- 실제 빌드 결과를 확인하세요

---

## 💡 참고 정보

### Visual Studio 2022 변경사항

- **MFC 헤더 구조 변경**: `new.h` → `<new>`
- **호환성**: `_INC_NEW` 매크로로 조건부 포함
- **해결**: 매크로 정의로 포함 시도 방지

### 기대 결과

수정 후 빌드하면:
- ✅ `new.h` 오류 해결
- ✅ 모든 소스 파일 컴파일 성공
- ✅ 실행 파일 생성

---

## ✅ 완료 확인

빌드 성공 시:

```
Release\MMMaster.exe 또는 Debug\MMMaster.exe 생성됨
```

오류가 지속되면:
1. `FIX_NEW_H_ERROR.md` 참조
2. Visual Studio 출력 창의 전체 오류 메시지 확인
3. 프로젝트 속성 재확인

